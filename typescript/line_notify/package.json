{
  "name": "line_notify_client",
  "version": "1.0.0",
  "description": "client application to notify line-notify",
  "private": true,
  "workspaces": {
    "packages": ["src/*"]
  },
  "scripts": {
    "start": "yarn workspace line_notify_front build && yarn workspace line_notify_server start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "s:start": "yarn workspace line_notify_server start",
    "c:build": "yarn workspace line_notify_front build"
  },
  "author": "",
  "license": "ISC"
}
