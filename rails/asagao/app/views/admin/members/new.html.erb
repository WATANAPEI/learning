<% @page_title = "new entry for member" %>

<h1><%= @page_title %></h1>

<%= form_for [:admin, @member] do |form| %>
    <%= render "shared/member_form", form:form %>
    <div><%= form.submit %></div>
<% end %>

